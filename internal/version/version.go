// Package version holds terman's version string, bumped by hand with
// each release.
package version

// Version is terman's current version. Bump this manually when cutting a
// release; there is no build-time injection or git-tag automation.
const Version = "0.6.1"
