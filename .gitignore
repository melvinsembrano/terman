# Binaries
/terman
*.exe~
*.dll
*.so
*.dylib

# Test binaries and coverage
*.test
*.out
coverage.txt

# Go build/vendor artifacts
/vendor/

# Go workspace files
go.work
go.work.sum

# terman's own local config/data (if run from within the repo)
.config/terman/

# OS/editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Ignore terman build artifacts (archives and checksums produced by build-binaries.sh)
terman-*.tar.gz
terman-*.zip
checksums.txt
# Old bare binary names (replaced by versioned archives)
terman-linux
terman-linux-arm
terman-mac
terman-mac-arm
terman.exe
